/** Client + server: default delegation team name and ordered member roles. */
export const DEFAULT_DELEGATION_TEAM_NAME = 'default';

export const DEFAULT_TEAM_MEMBER_NAMES = ['researcher', 'coder', 'reviewer'] as const;
